{% extends 'base.html.twig' %}

{% block title %}{{ 'edit_course'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'Edit_course'|trans }}</h1>

    {{ include('course/_form.html.twig', {'button_label': 'update'|trans}) }}
    <a href="{{ path('app_course_index_by_instructor', {'instructorId': app.user.id}) }}">{{ 'back_to_courses'|trans }}</a>
    {{ include('course/_delete_form.html.twig') }}
{% endblock %}
